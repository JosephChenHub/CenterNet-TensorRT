file(GLOB SRCS *.cu)


set(CU_SOURCES ${CU_SOURCES} ${SRCS})
set(CU_SOURCES ${CU_SOURCES} PARENT_SCOPE)


if (USE_GTEST)
    set(GTEST_SRCS ${CMAKE_CURRENT_SOURCE_DIR}/test.cc)

    set(GTEST_SOURCES ${GTEST_SOURCES} ${GTEST_SRCS})
    set(GTEST_SOURCES ${GTEST_SOURCES} PARENT_SCOPE)
endif(USE_GTEST)
